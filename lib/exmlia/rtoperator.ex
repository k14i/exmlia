defmodule Exmlia.rtoperator do

  def start(suffix) do
  end

  def rtoperator(suffix, tout) do
  end

  def store(pid, key, value, ttl, suffix) do
  end

  def store0([{_, {_, ip, port}}|t], {id, key}, value, ttl, suffix) do
  end

  def store0([{_, {_, ip, port}}|t], {id, key}, value, ttl, suffix) do
  end

  def store0([], _, _, _, _, _) do
  end

  def restore(pid, id, key, value, ttl, suffix) do
  end

  def restore0([{_, {_, ip, port}}|t], {id, key}, value, ttl, already, suffix) do
  end

  def restore0([{_, {_, ip, port}} = node|t], {id, key}, value, ttl, already, suffix) do
  end

  

end