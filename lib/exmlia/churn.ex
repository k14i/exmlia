defmodule Exmlia.churn do

  def start(port, boot_ip, boot_port, lambda, suffix) do
  end

  def stop(suffix) do
  end

  def run(port, boot_ip, boot_port, lambda, suffix) do
  end

  def exp_rand(lambda) do
  end

  def join(ip, port, n, pid) do
  end

  def join(_, _, _, _) do
  end

end